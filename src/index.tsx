import ReactDOM from 'react-dom/client';


import applicationConfig from './App';

const root = ReactDOM.createRoot(
    document.getElementById('root') as HTMLElement
);

root.render(applicationConfig);
